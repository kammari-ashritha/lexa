# Web Framework
fastapi==0.104.1
uvicorn[standard]>=0.24.0
pydantic==2.4.2

# Database & Environment
pymongo==4.6.0
python-dotenv==1.0.0

# AI Models (Updated to avoid the Hugging Face error)
sentence-transformers>=2.3.0

# Force CPU-only PyTorch to prevent Render out-of-memory crashes
--extra-index-url https://download.pytorch.org/whl/cpu
torch
